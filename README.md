# CNN Image Classifier (Dummy)

Proyek ini contoh aplikasi klasifikasi gambar menggunakan Streamlit.
Model yang digunakan masih dummy — nanti bisa diganti dengan model hasil training (`model_cnn.h5`).

## Struktur Folder